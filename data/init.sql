CREATE DATABASE IF NOT EXISTS `warren_bank`;

CREATE TABLE IF NOT EXISTS `warren_bank`.`users` (
    `id` VARCHAR(24) NOT NULL ,
    `name` VARCHAR(128) NOT NULL ,
    `email` VARCHAR(64) NOT NULL ,
    `cpf` VARCHAR(14) NOT NULL ,
    `password` VARCHAR(64) NOT NULL ,
    PRIMARY KEY (`id`),
    UNIQUE (`email`),
    UNIQUE (`cpf`)
) ENGINE = InnoDB;

CREATE USER 'warren_service'@'%' IDENTIFIED BY 'dKQxcfEEI*EoYLqB';

GRANT SELECT, INSERT, UPDATE, DELETE, FILE ON *.* TO 'warren_service'@'%' REQUIRE NONE WITH MAX_QUERIES_PER_HOUR 0 MAX_CONNECTIONS_PER_HOUR 0 MAX_UPDATES_PER_HOUR 0 MAX_USER_CONNECTIONS 0;